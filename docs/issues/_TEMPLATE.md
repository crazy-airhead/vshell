# ISSUE-XXXX — <标题>

> **编号**：XXXX　**状态**：🔴 未处理　**严重程度**：🚨 阻断 / ⚠️ 一般 / 💡 体验
> **发现日期**：YYYY-MM-DD　**相关任务**：<P ID 或模块，如 P2-1 / 编辑器>

## 问题描述

（一两句话说清是什么问题。）

## 复现步骤

1. （前置：处于何种模式 / 打开了什么）
2. （操作）
3. （观察到的现象）

## 期望行为

（应当发生什么。）

## 实际行为

（实际发生了什么，含报错 / 截图位置。）

## 影响范围

（受影响的功能 / 模块 / 用户场景。）

## 相关文件 / 符号

- `path/to/file.vue` — 说明
- `internal/xxx.Service.Method` — 说明

## 建议方案（可选）

（你认为的修复方向，或让 Claude 调查。）

## 解决记录

<!-- 约定：一个问题可能经多轮调整才最终收敛。单轮问题填下面三项即可；
     多轮的按「第 N 轮」追加，每轮记：反馈原话 → 根因/处理（文件级）→ 校验 → 遗留。
     范例见 0010 / 0012，约定见 README「解决记录约定」。 -->

- 修复提交 / PR：
- 改动：
- 校验：`go build ./...` / `go vet ./...` / `pnpm typecheck` 改动文件 0 新错
- 验收：

<!-- 多轮时这样逐条追加（每轮：反馈 → 根因 → 处理 → 校验 → 遗留）：
- 第一轮（反馈「…」）：
  - 根因：…
  - 处理：`path/to/file.vue` — …
  - 校验：typecheck 改动文件 0 错
  - 遗留：…（交下一轮）
- 第二轮（反馈「…」）：
  - …
-->

